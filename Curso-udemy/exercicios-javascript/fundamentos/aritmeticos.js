const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d;
const subtracao = d -b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2; // neste calculo ele me traz o resto da divisão entra a / 2 se o resto for 0
// quer dizer que aquele numero de 'a' é divisivel por 2 

console.log(soma, subtracao, multiplicacao, divisao, modulo);


console.log(-soma);//colocando esse menos na frente eu consigo converter o valor positivo para negativo